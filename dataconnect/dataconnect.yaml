specVersion: "v1beta"
serviceId: "projeto2"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "projeto2-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
